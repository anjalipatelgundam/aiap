class Queue:
    """
    A simple implementation of a Queue data structure (FIFO - First-In, First-Out).
    Supports enqueue, dequeue, peek, and is_empty operations.
    """
    def __init__(self):
        """
        Initializes an empty queue.
        """
        self._items = []

    def is_empty(self):
        """
        Checks if the queue is empty.

        Returns:
            bool: True if the queue is empty, False otherwise.
        """
        return len(self._items) == 0

    def enqueue(self, item):
        """
        Adds an item to the rear of the queue.

        Args:
            item: The item to be added to the queue.
        """
        self._items.append(item)

    def dequeue(self):
        """
        Removes and returns the item from the front of the queue.

        Returns:
            Any: The item removed from the front of the queue.

        Raises:
            IndexError: If the queue is empty.
        """
        if self.is_empty():
            raise IndexError("dequeue from empty queue")
        return self._items.pop(0) # Remove from the beginning of the list

    def peek(self):
        """
        Returns the item at the front of the queue without removing it.

        Returns:
            Any: The item at the front of the queue.

        Raises:
            IndexError: If the queue is empty.
        """
        if self.is_empty():
            raise IndexError("peek from empty queue")
        return self._items[0]

    def size(self):
        """
        Returns the number of items in the queue.

        Returns:
            int: The number of items in the queue.
        """
        return len(self._items)

    def __str__(self):
        """
        Returns a string representation of the queue.
        """
        return f"Queue: {self._items}"

# --- Test Cases ---
print("Initializing a new queue...")
my_queue = Queue()
print(f"Is queue empty? {my_queue.is_empty()}") # Expected: True

print("Enqueuing items: 10, 20, 30")
my_queue.enqueue(10)
my_queue.enqueue(20)
my_queue.enqueue(30)
print(my_queue)
print(f"Is queue empty? {my_queue.is_empty()}") # Expected: False
print(f"Queue size: {my_queue.size()}") # Expected: 3

print(f"Front item (peek): {my_queue.peek()}") # Expected: 10
print(my_queue) # Queue should remain unchanged

print(f"Dequeuing item: {my_queue.dequeue()}") # Expected: 10
print(my_queue)
print(f"Queue size: {my_queue.size()}") # Expected: 2

print(f"Dequeuing item: {my_queue.dequeue()}") # Expected: 20
print(my_queue)
print(f"Front item (peek): {my_queue.peek()}") # Expected: 30

print(f"Dequeuing item: {my_queue.dequeue()}") # Expected: 30
print(my_queue)
print(f"Is queue empty? {my_queue.is_empty()}") # Expected: True

print("\nAttempting to dequeue from an empty queue (should raise error):")
try:
    my_queue.dequeue()
except IndexError as e:
    print(f"Caught expected error: {e}")

print("\nAttempting to peek from an empty queue (should raise error):")
try:
    my_queue.peek()
except IndexError as e:
    print(f"Caught expected error: {e}")
